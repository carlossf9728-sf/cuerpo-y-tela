import { Escenas } from "@/components/escenas/Escenas";

// Portada: tres escenas a pantalla completa (inicio, galería y blog).
// Ver `src/components/escenas/Escenas.tsx`.
export default function Home() {
  return <Escenas />;
}
